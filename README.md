# ssh-bridge-server
A Node Server that bridges a SSH Tunnel with any other Server (like a HTML Server), so you can connect with a single port to two different services.

It detects the `SSH-2.0-OpenSSH` header to decide witch port to use.

*There is room for improvement*